# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Local data — NEVER commit real student PII. Synthetic data is regenerable.
data/synthetic/*
!data/synthetic/.gitkeep
data/real/
*.zip
*.csv
*.pdf

# Accounts / saved-projects SQLite DB (runtime data — may hold real applicant breakdowns)
*.db
data/accounts.db

# Generated schema is derived from code; keep the canonical copy only if intentionally committed.
# (run `make schema` to regenerate)

# Env / secrets
.env
*.key

# Local Claude Code config (machine-specific paths, dev creds)
.claude/
data/eval/jd_extraction_corrections.jsonl
