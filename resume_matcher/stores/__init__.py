"""Two physically separated data planes (plan boundary #2). The SCORING plane holds only
job-relevant features; the AUDIT plane holds voluntary self-ID protected attributes for
aggregate-only monitoring, with no join key back into scoring features."""
